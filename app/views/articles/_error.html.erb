<% if @article.errors.any? %>
  <div id="errors">
    <h2><%= pluralize(@article.errors.count, "error") %>
      prevented this article from saving</h2>
    <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>